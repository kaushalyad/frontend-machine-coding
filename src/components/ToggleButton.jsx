import React from 'react'

const ToggleButton = ({onClick}) => {
  return (
    <button style={{backgroundColor:"red",color:"grren"}} onClick={onClick}>ToggleButton</button>
  )
}

export default ToggleButton